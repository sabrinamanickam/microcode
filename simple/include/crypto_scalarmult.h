/* Stub for SUPERCOP's crypto_scalarmult.h.
 * SUPERCOP normally generates this header per implementation during its build
 * (mangling crypto_scalarmult → crypto_scalarmult_curve25519_donna_c64 etc.).
 * We vendor donna_c64's smult.c with our own macro rename, so an empty stub
 * is enough to satisfy the include line. */
